name: dependencies
description: install dependencies
runs:
  using: composite
  steps:
    - name: asdf install
      shell: bash
      run: |
        export PATH="$PATH:$HOME/.asdf/bin/"
        asdf install
