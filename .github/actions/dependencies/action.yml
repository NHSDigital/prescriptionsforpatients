name: dependencies
description: install dependencies
runs:
  using: composite
  steps:
    # - name: asdf install and plugins
    - name: asdf install
      shell: bash
      run: |
        sudo su - ubuntu
        asdf install
