{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "Sandbox",
  "main": "app.js",
  "author": "SAM CLI",
  "license": "MIT",
  "scripts": {
    "unit": "jest",
    "lint": "eslint . --ext .ts --max-warnings 0 --fix",
    "compile": "tsc",
    "test": "npm run compile && npm run unit",
    "check-licenses": "license-checker --failOn GPL --failOn LGPL --start ../.."
  },
  "dependencies": {
    "@aws-lambda-powertools/commons": "^1.11.0",
    "@aws-lambda-powertools/logger": "^1.10.0",
    "@middy/core": "^4.5.2",
    "@middy/input-output-logger": "^4.5.3",
    "@prescriptionsforpatients/middleware": "^1.0.0",
    "@prescriptionsforpatients/common_testing": "^1.0.0"
  }
}
