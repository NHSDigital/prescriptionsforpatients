{
  "name": "splunkProcessor",
  "version": "1.0.0",
  "description": "Processor lambda for converting cloudwatch logs to splunk",
  "main": "splunkProcessor.js",
  "author": "NHS Digital",
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage",
    "lint": "eslint . --ext .js --max-warnings 0 --fix",
    "check-licenses": "license-checker --failOn GPL --failOn LGPL --start ../.."
  },
  "dependencies": {
    "@aws-sdk/client-firehose": "^3.449.0",
    "@aws-sdk/client-kinesis": "^3.445.0",
    "aws-sdk": "^2.1494.0",
    "zlib": "^1.0.5"
  }
}
