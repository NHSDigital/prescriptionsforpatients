{
  "name": "getMyPrescriptions",
  "version": "1.0.0",
  "description": "Get my prescriptions",
  "main": "getMyPrescriptions.js",
  "author": "NHS Digital",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "unit": "POWERTOOLS_DEV=true NODE_OPTIONS=--experimental-vm-modules jest --no-cache --coverage",
    "lint": "eslint . --ext .ts --max-warnings 0 --fix",
    "compile": "tsc",
    "test": "npm run compile && npm run unit",
    "check-licenses": "license-checker --failOn GPL --failOn LGPL --start ../.."
  },
  "dependencies": {
    "@aws-lambda-powertools/commons": "^1.17.0",
    "@aws-lambda-powertools/logger": "^1.16.0",
    "@aws-lambda-powertools/parameters": "^1.16.0",
    "@aws-sdk/client-secrets-manager": "^3.454.0",
    "@middy/core": "^5.0.3",
    "@middy/input-output-logger": "^5.0.3",
    "@prescriptionsforpatients/middleware": "^1.0.0",
    "@prescriptionsforpatients/spineClient": "^1.0.0"
  },
  "devDependencies": {
    "axios-mock-adapter": "^1.22.0",
    "@prescriptionsforpatients_common/testing": "^1.0.0"
  }
}
